{
  "name": "veriftout-v2",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "functions"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
